# Tmux Configuration
set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

# Change prefix key
unbind C-b
set -g prefix C-s
bind C-s send-prefix

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# Status bar configuration
set -g status on
set -g status-interval 1
set -g status-justify left
set -g status-left-length 40
set -g status-right-length 150
set -g status-left '#[fg=#5de4c7,bg=#1b1e28] #S #[fg=#1b1e28,bg=#5de4c7] '
set -g status-right '#[fg=#a6accd,bg=#1b1e28] %Y-%m-%d %H:%M #[fg=#5de4c7,bg=#1b1e28] #(whoami)@#h '
set -g status-style 'fg=#a6accd,bg=#1b1e28'

# Window status
setw -g window-status-format ' #I:#W#F '
setw -g window-status-current-format ' #I:#W#F '
setw -g window-status-current-style 'fg=#1b1e28,bg=#5de4c7,bold'
setw -g window-status-style 'fg=#a6accd,bg=#1b1e28'

# Pane border
set -g pane-border-style 'fg=#3d59a1'
set -g pane-active-border-style 'fg=#5de4c7'

# Message text
set -g message-style 'fg=#a6accd,bg=#1b1e28'

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

# Initialize TPM (auto-install if missing)
if-shell 'test ! -d ~/.tmux/plugins/tpm' \
    'run "git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm"' \
    'run "~/.tmux/plugins/tpm/tpm"'

# Vi mode
set-window-option -g mode-keys vi
# Keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Split windows
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"